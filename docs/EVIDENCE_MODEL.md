# Evidence Model

Statuses: `NOT_RUN`, `RUNNING`, `PASS`, `FAIL`, `ABORTED`, `UNSUPPORTED`, `STALE`.

Evidence sources: deterministic local computation, native process, filesystem verification, explicit test fixture, or user action.

A process spawning successfully is not a successful run. A filename existing is not a verified checkpoint. A decreasing loss is not broad model quality.
